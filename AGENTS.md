# AI・人間共通の作業入口

このリポジトリは、おざけん（小澤健祐）の講演資料と公開サイトを管理します。
Codex、Claude、その他のAIは、作業前にこのファイルと担当分野の手順を読んでください。

## 最初に読むもの

1. [運用手順](docs/operations.md)：生成元、資料作成・改訂、暗号化、プレビュー、検証、公開、復旧。
2. [講演・印刷のデザイン規約](docs/lecture-design.md)：2026年9月の本人指定。テンプレートと講演資料を変更するときの基準。
3. 資料本文を作る・直す場合は [ozaken-shiryo](.claude/skills/ozaken-shiryo/SKILL.md) と [ozaken-hyoka](.claude/skills/ozaken-hyoka/SKILL.md)。
4. 週次記事は [週次運用](docs/weekly-system.md)、調査は [スイープ手順](docs/weekly-sweep-runbook.md)、メルマガは [配信運用](docs/newsletter-system.md)。

最新のユーザー指示と、この会話で確定した対象範囲を優先します。旧手順の操作式パーツ・段階的な本文表示は、講演・印刷の指定がある資料では使いません。既に回答・許可を得ていることを再確認しません。

## 作業の基本

- `git status --short`、ブランチ、対象の生成元を確認する。他の作業の変更を上書きしない。
- 暗号化HTMLの暗号文を直接編集しない。生成元を修正し、対象のビルダーで再生成する。
- `template.html` は `build_template_gallery.py`、`01_concept/use-to-delegate.html` は `delegation_pilot/build.py` が現在の生成入口。古い `gen_*.py` だけで上書きすると今回のデザインが失われる。
- 講演の本文・数値・図の全情報は最初から表示する。クリック切り替え、開閉、＋、再生ボタンを追加しない。列ホバーは見た目の強調だけに使う。
- 現行テンプレートの広い余白と情報密度を標準とする。見出し・図・補足を分け、空間を埋めるために要素を足さない。画面幅・印刷ごとの数値は [余白の基準](docs/lecture-design.md#余白の基準) に従う。
- 背景・光・接続線を動かす。文字を読める位置に固定し、動きを減らす設定・画面外停止・印刷時停止を保つ。
- PR / ST / GO / EN、その他の隠しコマンド、既存QR、関連資料リンク、暗号化のラップ鍵 `W` を維持する。隠しコマンドは認証の代わりではない。
- マスター・共通・個別パスワード、トークン、名簿、復号したプレビューをGitに追加しない。実値をドキュメントやコマンド例に記録しない。
- 対象がテンプレートだけなら、個別資料やトップを一括更新しない。共通スクリプトの全件適用は依頼範囲と影響を確認してから行う。
- 生成・暗号化・Gitへの記録・本番配信を区別する。`publish.py` や `--publish` はローカル生成であり、デプロイではない。`main` へのpushは本番更新になる。
- 本番反映の指示・許可がある場合は検証まで済ませて進める。ローカル確認のみの場合は本番を変えない。メール送信には送信先・内容についての明示的な依頼が必要。
- 実表示で重なり・はみ出し・アニメーションを確認する。印刷CSSだけの確認を「PDF確認済み」と報告しない。
- 操作は実行環境で許可されたツールを使う。既存スクリプトにブラウザ操作が書いてあっても、利用中の環境の制約を優先する。

## 引き継ぎ

作業を終えるときは、変更した生成元と生成物、確認した範囲、残る制約、コミット、本番反映の有無を短く記録します。新しい運用を決めたら該当する手順書を同じ変更で更新してください。
